https://github.com/namazovanton/homework_21.git

1) Некорректные названия файлов, по которым непонятно,
 что и где находится.

2) В файле "under_main.py" неиспользуемый импорт в строчке №2

3) В файле "main.py" в строчке №22 код можно переписать
на более короткий вариант:
if 'stop' or 'стоп' in user_input:

4) В файле "under_main.py" в строчке №32 код можно переписать
на более короткий вариант:
if self.carry_from or self.take_to not in storages:

5) В файле "upper_main.py" такие функции,
как "get_free_space", "get_items" и "get_unique_items_count"
можно вынести в конструктор
в классах "AbstractionStorage" и "BaseStorage"

6) В файле "upper_main.py" в строчках 32 и 41 необходим комментарий
по проверкам, которые осуществляет метод.

7) В файле "under_main.py" в строчке 31 необходим
"комментарий-напоминание", о том что именно мы должны проверить в запросе.

8) В файле "main.py" в строчках 27 и 30 стоит прокомментировать,
что именно происходит со складом и магазином.